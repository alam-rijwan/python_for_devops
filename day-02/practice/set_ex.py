info = {} # empty dic
days = set() # empty set
days = {"Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"} # set of days of the week

print(days) # Printing the set of days
print(type(days)) # Printing the type of the variable days
# Set is a collection of unique elements. It is unordered, mutable and unindexed. It is defined by using curly braces {}.
# Sets do not allow duplicate elements and do not maintain the order of elements.
# Sets are useful when we want to store a collection of unique elements and perform operations like union, intersection, difference, etc.
# Sets are defined by using curly braces {} or the set() constructor.
# 1st method to create a set
print(type(info)) # Printing the type of the variable info